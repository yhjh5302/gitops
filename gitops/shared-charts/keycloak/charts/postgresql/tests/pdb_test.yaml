# SPDX-License-Identifier: Apache-2.0
suite: PodDisruptionBudget
templates:
  - pdb.yaml
release:
  name: test
  namespace: default
tests:
  - it: should not create PDB by default in standalone mode
    asserts:
      - hasDocuments:
          count: 0

  - it: should create PDB when enabled in standalone mode
    set:
      pdb.enabled: true
    asserts:
      - isKind:
          of: PodDisruptionBudget
      - equal:
          path: spec.minAvailable
          value: 1

  - it: should create PDB by default in replication mode
    set:
      architecture: replication
    asserts:
      - isKind:
          of: PodDisruptionBudget

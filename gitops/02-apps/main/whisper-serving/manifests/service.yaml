apiVersion: v1
kind: Service
metadata:
  name: whisper-serving
  namespace: private-assistant
spec:
  ports:
    - port: 8000
      targetPort: 8000
      name: http
  selector:
    app: whisper-serving

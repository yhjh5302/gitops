apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: qwen-model-cache-pvc
  namespace: private-assistant
  annotations:
    helm.sh/resource-policy: keep
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 20Gi

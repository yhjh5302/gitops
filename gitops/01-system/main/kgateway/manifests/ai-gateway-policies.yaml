apiVersion: gateway.kgateway.dev/v1alpha1
kind: GatewayParameters
metadata:
  name: kgateway-override
  namespace: kgateway
spec:
  kube:
    service:
      type: NodePort
      ports:
        - port: 80
          nodePort: 32080
        - port: 443
          nodePort: 32443
